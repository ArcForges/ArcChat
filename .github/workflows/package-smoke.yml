name: package-smoke

on:
  workflow_dispatch:

jobs:
  package-smoke-placeholder:
    runs-on: windows-2025
    steps:
      - uses: actions/checkout@v6
      - run: echo "Package smoke lands in NC16/NC17."
