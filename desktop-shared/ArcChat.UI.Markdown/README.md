# ArcChat.UI.Markdown

Owns the streaming Markdown UI foundation for the NextChat rewrite. This placeholder exists so Step NC01 can validate the monorepo shape before feature code lands in later steps.
